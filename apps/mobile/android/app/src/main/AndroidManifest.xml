<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Push (FCM): obrigatoria pra exibir notificacoes no Android 13+ (API 33).
         Em versoes anteriores e concedida automaticamente. O app pede em runtime
         via messaging().requestPermission() (ver features/notifications/setup.ts). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ===== Alarmes locais de remedio (notifee, M6/B8) ===== -->
    <!-- Despertar no horario EXATO da dose (Android 12+, API 31). USE_EXACT_ALARM
         e auto-concedida e dispensa o prompt; SCHEDULE_EXACT_ALARM e o fallback
         pedido em runtime via notifee.openAlarmPermissionSettings() em devices que
         revogam a exata. Justificativa Play Store: lembrete de medicacao infantil. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Alarme abre por cima da lockscreen como um despertador (Android 14+). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Reagendar os alarmes apos o usuario reiniciar o celular. O notifee
         re-registra os triggers automaticamente no BOOT_COMPLETED. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Manter a CPU acordada no instante em que o alarme dispara. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Vibrar junto com o alarme. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!--
          Deep link: bebecare://...
          Usado por links de convite de familia: bebecare://invite/ABC123
          Quando o user toca o link, Android abre o app na MainActivity, que
          encaminha o intent pro React Navigation linking config (ver
          src/app/navigation/RootNavigator.tsx).
        -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="bebecare" />
        </intent-filter>
      </activity>
    </application>
</manifest>
