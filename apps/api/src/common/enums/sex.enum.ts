// Sexo biológico do bebê — usado em cálculos do gráfico de crescimento
// (curvas da OMS variam por sexo) e em saudações/iconografia do app.
export enum Sex {
  MALE = 'male',
  FEMALE = 'female',
}
